<div class="container">
    <!-- Your logo -->
    <img src="{{ asset('assets/images/logo/logo3.png') }}" alt="Logo" class="logo">
    
    <!-- Reclamation details -->
    <div class="reclamation-details">
        <h2>Reclamation Details</h2>
        <p><strong>ID:</strong> {{ reclamation.id }}</p>
        <p><strong>Object:</strong> {{ reclamation.objet }}</p>
        <p><strong>Type:</strong> {{ reclamation.typeDeReclamation }}</p>
        <p><strong>Description:</strong> {{ reclamation.descriptionReclamation }}</p>
        <p><strong>Date:</strong> {{ reclamation.date ? reclamation.date|date('Y-m-d') : '' }}</p>
        <!-- Add more reclamation details as needed -->
    </div>
</div>
