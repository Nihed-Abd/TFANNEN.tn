{% extends 'base.html.twig' %}

{% block title %}Competitiondesigner{% endblock %}

{% block body %}
    <h1>Competitiondesigner</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Name</th>
                <td>{{ competitiondesigner.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ competitiondesigner.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ competitiondesigner.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_competitiondesigner_index') }}">back to list</a>
    &nbsp;&nbsp;

    <a href="{{ path('app_competitiondesigner_edit', {'id': competitiondesigner.id}) }}">edit</a>

    {{ include('competitiondesigner/_delete_form.html.twig') }}
{% endblock %}
